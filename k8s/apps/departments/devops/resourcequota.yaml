apiVersion: v1
kind: ResourceQuota
metadata:
  name: devops-quota
  namespace: devops
spec:
  hard:
    pods: "20"
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "8"
    limits.memory: "16Gi"